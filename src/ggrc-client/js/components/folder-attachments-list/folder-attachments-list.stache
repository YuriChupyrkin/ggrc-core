{{!
    Copyright (C) 2019 Google Inc.
    Licensed under http://www.apache.org/licenses/LICENSE-2.0 <see LICENSE file>
}}
<div class="folder-attachments-list">
    <related-documents vm:instance:from="instance" vm:kind:from="'FILE'" vm:isLoading:to="isListLoading">
        <div class="folder-attachments-list__heading">
            <spinner class="folder-attachments-list__spinner" vm:toggle:from="showSpinner"></spinner>
            <h6>{{title}}</h6>
        </div>
            <ggrc-gdrive-folder-picker vm:hideLabel:from="true"
                                       vm:readonly:from="readonly"
                                       vm:instance:from="instance"
                                       vm:current_folder:to="*currentFolder"
                                       vm:folder_error:bind="folderError"
            ></ggrc-gdrive-folder-picker>
        <object-list vm:items:from="documents" vm:emptyMessage:from="noItemsText" vm:showMore:from="showMore">
            <editable-document-object-list-item vm:document:from="{.}">
            {{#unless readonly}}
                <unmap-button
                    vm:destination:from="instance"
                    vm:source:from="document"
                    vm:isUnmapping:to="../../../../isUnmapping">
                    <action-toolbar-control>
                      <i class="fa fa-trash"></i>
                    </action-toolbar-control>
                </unmap-button>
            {{/unless}}
            </editable-document-object-list-item>
        </object-list>

    {{#unless readonly}}
    {{#is_allowed 'update' instance context='for'}}
    {{#if *currentFolder}}
    {{^if folderError}}
    <span class="attachments-list-action">
        <button
            type="button"
            rel="tooltip"
            data-placement="bottom"
            data-original-title="{{tooltip}}"
            class="btn btn-small btn-white"
            data-toggle="unified-mapper"
            data-join-object-id="{{instance.id}}"
            data-join-object-type="{{instance.class.model_singular}}"
            data-join-option-type="Document">
            Attach
        </button>
    </span>
    {{/if}}
    {{/if}}
    {{/is_allowed}}
    {{/unless}}
    </related-documents>
</div>
